# Auto-generated by the Swift Package workflow (swift.yml). Do not edit manually.
Pod::Spec.new do |s|
  s.name         = 'ChordSketch'
  s.version      = '{{VERSION}}'
  s.summary      = 'ChordPro file format parser and renderer'
  s.description  = <<-DESC
    ChordSketch is a Rust implementation of the ChordPro file format.
    It parses .cho files and renders them to text, HTML, or PDF output.
    This pod provides the pre-built chordsketchFFI XCFramework for
    iOS and macOS.
  DESC
  s.homepage     = 'https://github.com/koedame/chordsketch'
  # The source is the XCFramework zip, which holds no LICENSE file, so the
  # licence is inlined: a `:file` that `pod spec lint` cannot find is a
  # lint warning, and trunk refuses warnings for an open-source pod.
  s.license      = { :type => 'MIT', :file => 'LICENSE' }
  s.author       = { 'koedame' => 'koedame@users.noreply.github.com' }
  s.source       = {
    :http => 'https://github.com/koedame/chordsketch/releases/download/v{{VERSION}}/chordsketch-xcframework.zip'
  }

  s.ios.deployment_target = '15.0'
  s.osx.deployment_target = '12.0'

  s.vendored_frameworks = 'chordsketchFFI.xcframework'
end
