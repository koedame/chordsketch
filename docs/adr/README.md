# Architecture Decision Records

This directory contains the architecture decision records (ADRs) for
ChordSketch. An ADR captures a single, significant architectural or
operational decision together with the context that produced it, the
alternatives considered, and the consequences accepted.

ADRs exist to preserve **why** a decision was made, not just **what** was
decided. The codebase, git history, and issue tracker already record the
**what**; an ADR exists when the **why** would otherwise be lost.

## When to write an ADR

Write an ADR when any of the following are true:

- A decision intentionally **declines** to do work that an open issue
  proposes (e.g. an upstream-blocked migration), and that decision should
  outlive the issue's closure.
- A decision rules out an alternative whose case is non-obvious enough that
  a future contributor might re-propose it without the historical context.
- A decision establishes a project-wide convention that the rules in
  `.claude/rules/` do not already cover.

Routine code changes do **not** need an ADR. The bar is "would a reasonable
future contributor reach the wrong conclusion if this rationale were
missing?"

## File naming

ADR files are named `NNNN-kebab-case-title.md`, where `NNNN` is a
zero-padded sequence number assigned in creation order. Numbers are
**never reused**, even if an ADR is later superseded.

## Template

Each ADR follows this structure:

```markdown
# NNNN. Short title in sentence case

- **Status**: Accepted | Superseded by ADR-NNNN | Deprecated
- **Date**: YYYY-MM-DD

## Context

What problem prompted this decision? What constraints, prior art, or
upstream limits define the space of possible answers?

## Decision

The chosen course of action, stated unambiguously.

## Rationale

Why this option, given the context. Cite evidence (URLs, commit hashes,
issue numbers) so the reasoning can be re-verified later.

## Consequences

The trade-offs accepted by the decision — both positive and negative.
Mitigations for the negatives, if any.

## Alternatives considered

Other options that were on the table and why they were rejected.

## References

Issues, PRs, external documentation, and any "watch signals" that should
prompt revisiting the decision.
```

Once the ADR is committed, its **Status** is locked. If the decision later
changes, write a new ADR that supersedes the old one and update the old
ADR's Status line to `Superseded by ADR-NNNN`.

## Index

| ADR | Title | Status |
|---|---|---|
| [0001](0001-kotlin-maven-central-publishing-credentials.md) | Kotlin Maven Central publishing credentials | Accepted (2026-04-11) |
| [0002](0002-aur-smoke-coverage-exemption.md) | AUR install command is exempt from readme-smoke coverage | Accepted (2026-04-18) |
| [0003](0003-github-merge-queue.md) | GitHub Merge Queue replaces the auto-update-branch cascade | Superseded by ADR-0015 (2026-04-22) |
| [0004](0004-unsafe-eval-for-web-tree-sitter-emscripten.md) | `'unsafe-eval'` in the desktop CSP for `web-tree-sitter` | Accepted (2026-04-24) |
| [0005](0005-tauri-updater-key-management.md) | Tauri updater key management (Ed25519, no password) | Superseded by ADR-0007 (2026-04-25) |
| [0006](0006-desktop-webview-trust-boundary.md) | Desktop WebView is trusted; custom commands are not capability-gated | Accepted (2026-04-24) |
| [0007](0007-tauri-updater-key-with-password.md) | Tauri updater key requires a non-empty password | Accepted (2026-04-25) |
| [0008](0008-npm-publishing-is-local.md) | npm publishing is a maintainer-local manual operation | Superseded by ADR-0069 (2026-09-15; Decision 6, the package names, stands) |
| [0009](0009-release-event-cascade-credential.md) | Release event cascading requires a non-GITHUB_TOKEN credential | Superseded by ADR-0039 (2026-04-26) |
| [0010](0010-image-path-resolution-stays-strict.md) | Image path resolution stays strict (declines R6.100 `~` and folder-next-to-song) | Accepted (2026-04-28) |
| [0011](0011-html-styles-stay-inline.md) | HTML styles stay inline-per-element (declines R6.100 `default/screen/print` + `html.style.embed`) | Accepted (2026-04-28) |
| [0012](0012-macports-portfile-cargo-crates-tag-relative.md) | MacPorts Portfile cargo.crates is tag-relative, not HEAD-relative | Accepted (2026-04-29) |
| [0013](0013-conditional-bot-driven-merge.md) | Bot-driven merge is allowed under explicit session permission | Accepted (2026-04-29; condition 4 updated by ADR-0015; clause 1 extended for unattended Dependabot merge by ADR-0024; clause 1 generalized by ADR-0047; condition 4 returned to the merge queue by ADR-0079) |
| [0014](0014-bravura-glyphs-as-svg-paths.md) | Bravura SMuFL glyphs ship as inline SVG paths, not as a bundled font | Accepted (2026-05-01) |
| [0015](0015-disable-github-merge-queue.md) | Disable GitHub Merge Queue (supersedes ADR-0003) | Superseded by ADR-0079 (2026-05-03) |
| [0016](0016-dependabot-review-skill.md) | Dependabot review moves from a CI bot to a session skill; major bumps are no longer suppressed | Accepted (2026-05-03; unattended scheduled run added by ADR-0024; patch and minor updates grouped by ADR-0075) |
| [0017](0017-react-renders-from-ast.md) | React surface renders from AST; Rust HTML renderer demoted to static-output | Accepted (2026-05-10; consumer classification partially updated by ADR-0022) |
| [0018](0018-phase-based-shell-orchestrated-workflows.md) | Phase-based shell-orchestrated workflows (declines cc-wf-studio / n8n / Agent SDK as substitutes) | Accepted (2026-05-16) |
| [0019](0019-batch-mode-autopilot-issue.md) | Batch-mode autopilot-issue workflow (one PR aggregates multiple eligible issues per round) | Accepted (2026-05-17) |
| [0020](0020-ireal-pro-react-surface.md) | iReal Pro React surface is native React (option (b) MVP), not a `ui-irealb-editor` wrapper | Accepted (2026-05-19) |
| [0021](0021-docs-site-co-located-with-playground.md) | Docs site is co-located with the playground (option (a), not Docusaurus / VitePress / subdomain) | Accepted (2026-05-19) |
| [0022](0022-react-as-canonical-preview-surface.md) | React as the canonical preview surface; `@chordsketch/ui-web` retired | Accepted (2026-05-20) |
| [0023](0023-capo-transposes-displayed-chords.md) | `{capo}` directive transposes displayed chords | Accepted (2026-05-24) |
| [0024](0024-scheduled-dependabot-merge.md) | Scheduled unattended Dependabot review-and-merge, all bump types (extends ADR-0013 clause 1) | Accepted (2026-05-25; audit and diff-sanity gate applied per dependency of a grouped PR by ADR-0075; condition 4 merges through the queue by ADR-0079) |
| [0025](0025-build-time-syntax-highlighting-shiki.md) | Build-time syntax highlighting for the docs site uses Shiki (preserves ADR-0021 zero-JS posture; reuses in-repo ChordPro TextMate grammar) | Accepted (2026-05-28) |
| [0026](0026-horizontal-chord-diagram-default-string-order.md) | Horizontal chord diagrams render reader-view only (high pitch on top, matches tablature stave order); player-view is not supported | Accepted (2026-05-30) |
| [0027](0027-inline-hover-compact-chord-diagrams.md) | Inline / hover chord diagrams use a dedicated compact layout (chordsketch `{diagrams: inline}` / `{diagrams: hover}` extension; not a CSS scale) | Accepted (2026-05-31) |
| [0028](0028-shared-directive-catalog.md) | Shared directive catalog in `chordsketch-chordpro` is the single source of truth for directive-name + value completion across LSP / web / playground | Accepted (2026-05-31) |
| [0029](0029-react-ui-primitives-package.md) | Design-system React primitives live in a wasm-free `@chordsketch/react-ui`; `@chordsketch/react` stays domain-only and does not re-export them | Accepted (2026-06-01) |
| [0030](0030-chord-diagram-fret-number-axis.md) | Chord diagrams label the full visible fret range by default across SVG / HTML / PDF; the axis subsumes the legacy single base-fret label, compact keeps it | Accepted (2026-06-15); labelling position superseded by ADR-0031, compact carve-out by ADR-0032 |
| [0031](0031-chord-diagram-fret-numbers-at-cell-centres.md) | Chord-diagram fret numbers sit at fret-cell centres (press positions, `1 2 3 …`, no nut `0`), not on fret lines; supersedes ADR-0030's labelling-position sub-decision | Accepted (2026-06-15); compact carve-out superseded by ADR-0032 |
| [0032](0032-compact-diagrams-show-fret-number-axis.md) | Inline / hover (compact) diagrams also draw the press-position fret-number axis (smaller font), not the legacy single label; supersedes ADR-0030/0031's compact carve-out | Accepted (2026-06-15) |
| [0033](0033-canonical-key-directive-notation.md) | Canonical `{key}` notation + a single strict key parser (`parse_key`); malformed keys (`G m`, `Gminor`, `G minor`, `G7`) warn and render verbatim instead of being silently mis-classified | Accepted (2026-06-17); strict-input clause superseded by ADR-0034 |
| [0034](0034-lenient-key-input-canonical-render.md) | Lenient `{key}` input, canonical render: accept the common human key spellings (`G minor`, `G m`, `Gminor`, `G major`, …) and normalise them to a canonical form on every render surface while leaving the editor source untouched; only non-keys (`G7`, `H`) still warn (supersedes ADR-0033's strict-input clause) | Accepted (2026-06-17); canonical-form clause superseded by ADR-0035 |
| [0035](0035-spelled-out-canonical-key-notation.md) | Spelled-out canonical `{key}` notation: the rendered canonical form is `G major` / `G minor` / `C dorian` (was `Gm` / `G`), parallel with the modal form, via the shared `key::quality_word` so every surface agrees; input stays lenient (supersedes ADR-0034's canonical-form clause) | Accepted (2026-06-17) |
| [0036](0036-real-bravura-glyphs-for-key-and-staff.md) | Real Bravura SMuFL outlines (gClef / sharp / flat / notehead) for the `{key}` chip + chord-tone staff, baked as SVG paths via the ADR-0014 generator across render-html + `@chordsketch/react`; no JS notation engine (renderer-parity), Verovio recorded as a future watch signal | Accepted (2026-06-22) |
| [0037](0037-explicit-chord-extension-notation.md) | Explicit chord-extension notation: the editor produces only unambiguous forms (`G7(13)` / `G7(9,11,13)`, never bare `G13`; `Cadd9` / `C7(9)`, never `C(9)`) via orthogonal triad × seventh × tension controls; ambiguous notation still parses and renders but warns toward the explicit spelling (sister to ADR-0034's `validate_keys`), source never auto-rewritten | Accepted (2026-06-22) |
| [0038](0038-single-sourced-design-tokens.md) | Design tokens stay single-sourced in `design-system/tokens.css`; a bespoke zero-dependency `scripts/build-tokens.mjs` projects it to the hand-mirrored derived copies (the per-package `--cs-*` blocks and the iReal bare block) between `@generated` markers, with a CI diff guarding drift; namespaces and scopes preserved, single light theme | Accepted (2026-06-26) |
| [0039](0039-release-fan-out-is-an-explicit-call-graph.md) | Release fan-out is an explicit `workflow_call` graph rooted at `release.yml`, not a `release: [published]` broadcast; tag-namespace routing and publish-before-verify ordering become structural, `release-verify` moves to a daily convergence sweep, `vscode-extension.yml` stops masking failures, and `RELEASE_DISPATCH_TOKEN` is retired (supersedes ADR-0009; amended by ADR-0067, which folds `swift.yml` into the graph) | Accepted (2026-08-30) |
| [0040](0040-external-tool-tests-are-not-run-in-ci.md) | External-tool integration (abc2svg / LilyPond / MuseScore / Perl ChordPro) is not covered by CI; the never-run, dispatch-only `Extended Tests` workflow is removed and the `#[ignore]` tests stay as local on-demand checks | Accepted (2026-08-30) |
| [0041](0041-readme-smoke-scoped-to-readme-prs.md) | README install smoke tests run on PRs only when the README system changes (`README.md`, the command snapshot, the fixtures, the `cli-render-smoke` composite, the workflow); the daily cron stays the detector for registry-side regressions | Accepted (2026-08-30) |
| [0042](0042-action-pins-must-be-default-branch-ancestors.md) | Action SHA pins must name a commit reachable from the upstream default branch (a rewritten-branch tip breaks Dependabot's `github_actions` job and can be garbage-collected out from under every workflow); `dtolnay/rust-toolchain` moves to a `master` pin + explicit `toolchain:` | Accepted (2026-08-30) |
| [0043](0043-issue-not-required-for-prs.md) | A GitHub Issue is not a prerequisite for a change; branches without an issue use `{type}/{short-kebab-case}` and PR bodies without `Closes #N` are valid (issues stay the place for user reports, planning, and `type:tracking` umbrellas) | Accepted (2026-08-30) |
| [0044](0044-pre-1.0-breaking-changes-are-expected.md) | Pre-1.0 releases may break compatibility without a deprecation cycle (public API, rendered output, CLI flags, binding wire shape) and an MSRV raise is not held for `1.0.0`; the workspace floor moves 1.85 → 1.88 to unblock the `uniffi` / `pdf-extract` bumps | Accepted (2026-08-31) |
| [0045](0045-retire-one-pr-at-a-time.md) | Retire the one-PR-at-a-time serialisation rule: no cap on open PRs against `main`, the `autopilot-issue` HALT gate is removed, and the macOS 5-job ceiling keeps its home in `ci-parallelization.md` §5 as advice rather than a prohibition | Accepted (2026-09-01) |
| [0046](0046-linux-release-binaries-target-an-old-glibc.md) | Every Linux artifact — release archives, Node addon, gem and JAR native libraries — is linked against an old sysroot (`cross`, or `--use-napi-cross` for napi-rs) under one glibc 2.18 support floor, enforced on PRs against the release matrix and against each workflow's built artifacts before its publishing job; bottling Homebrew, swapping in musl, and documenting a 2.39 floor are all rejected (amended by ADR-0056, which carves the desktop bundles out to their own 2.35 floor) | Accepted (2026-09-02) |
| [0047](0047-merge-authorization-may-precede-the-work.md) | Merge authorization may be granted before the work begins: ADR-0013 condition (1) becomes "authorization to merge", satisfied by a grant made with a self-contained work unit (covering the PRs the assistant opens for it), by a named PR, or by explicit permission in the active session; inference and carry-over stay prohibited and conditions (2)–(4) are unchanged | Accepted (2026-09-02) |
| [0048](0048-scheduled-rustsec-audit.md) | RustSec advisories are detected by a daily `cargo audit` sweep that maintains one rolling tracking issue instead of failing; a pull request fails only for a vulnerability its own diff introduces, findings are labelled runtime vs dev-only, and every `.cargo/audit.toml` ignore entry carries an enforced `why:` and `review-by:` | Accepted (2026-09-01) |
| [0049](0049-chocolatey-rollup-reports-pending-as-its-own-verdict.md) | The post-release rollup's Chocolatey check reports three verdicts — OK once the version is installable from the feed `choco install` reads, PENDING while community moderation holds it, FAIL when the push never landed — because green must mean installable and red must be actionable | Accepted (2026-09-01) |
| [0050](0050-windows-preview-handler-is-installer-registered.md) | The Windows Explorer preview handler ships inside the desktop installer and is registered declaratively by WiX / NSIS at the installer's own scope (`HKMU` / `SHCTX`), not by a self-registering `DllRegisterServer`; the pane is a WebView2 control showing `chordsketch-render-html`'s fragment output under a `default-src 'none'` CSP, with the WebView2 profile in `LocalLow` and `IPreviewHandlerVisuals` deliberately unimplemented | Accepted (2026-09-02) |
| [0051](0051-inline-chord-diagrams-centre-on-the-lyric-position.md) | Inline chord diagrams (`{diagrams: inline}`) are centred on the lyric position their chord marks by shifting each cell half its own width, a paint-time transform that leaves every block width and lyric position untouched and cannot make diagrams collide on any instrument; cross-axis centring (which moves the lyric) and dropping the diagram from the width calculation (which makes diagrams overlap) are rejected | Accepted (2026-09-02) |
| [0052](0052-svelte-bindings-publish-sources.md) | `@chordsketch/svelte` publishes preprocessed `.svelte` sources plus generated `.d.ts` via `svelte-package` instead of a `tsup` ESM + CJS bundle, because the host application's own Svelte compiler is what turns a component into JavaScript and a CJS half would have no consumer; `vitePreprocess({ script: true })` (not the default) strips TypeScript and the `dist/` check in `svelte.yml` guards it | Accepted (2026-09-03) |
| [0053](0053-framework-demos-live-in-the-playground.md) | The Vue and Svelte bindings get live demo routes inside the existing playground Vite project (`/chordsketch/vue/`, `/chordsketch/svelte/`) rather than the `examples/docs-site/` Next.js app #2046 asked for; ADR-0021 already answered the hosting question, React's live surface stays the ChordPro playground, and the pages double as the only browser coverage either binding has. Lighthouse accessibility lands at 100, performance is recorded as unmet at parity with the playground | Accepted (2026-09-03) |
| [0054](0054-tertiary-ink-is-not-a-body-text-tone.md) | `--ink-500` / `--text-tertiary` (#8A8790) keeps its value and is documented as a non-text / large-text / disabled tone rather than a body-text one: at 3.53:1 it clears WCAG 1.4.11's 3:1 but not 1.4.3's 4.5:1, and no lighter tone both passes and stays visually distinct from `--ink-600` (the lightest AA-passing ink is #726F78, nine channel steps away), so small copy moves to `--text-secondary` instead of the token changing under every external consumer | Accepted (2026-09-03) |
| [0055](0055-docs-shiki-theme-matches-the-code-surface.md) | The docs Shiki theme is `github-dark-default`, picked because the build strips the theme's own background and paints `--cs-ink-1000` (#0A0A0B): the theme must be designed for a near-black surface and every token colour it emits over the corpus must clear 4.5:1, which `github-dark`'s #6A737D comments (4.11:1) did not | Accepted (2026-09-03) |
| [0056](0056-desktop-bundles-target-ubuntu-2204.md) | The Linux desktop bundles are built on a pinned `ubuntu-22.04` runner under their own glibc 2.35 floor, separate from ADR-0046's 2.18: a Tauri app links the host's webkit2gtk, `cross`'s Ubuntu 16.04 image cannot build it, and 22.04 is the oldest image carrying webkit2gtk 4.1 — which is also what excludes Debian 11 and RHEL at any glibc. Both desktop matrices' runner pins are asserted per PR and all three bundle formats (including the AppImage's ~160 bundled system libraries) are measured before upload (amended by ADR-0057: the RHEL exclusion covers EL 8 and EL 9 — EPEL packages webkit2gtk 4.1 for EL 10, where the `.rpm` installs) | Accepted (2026-09-04) |
| [0057](0057-desktop-rpm-stays-a-webkit2gtk-41-channel.md) | The desktop `.rpm` keeps being published as what it actually is — a webkit2gtk 4.1 channel serving Fedora and EL 10 with EPEL, measured rather than assumed — instead of being dropped or duplicated against webkit2gtk 4.0 / libsoup2: EL 8 and EL 9 package 4.0 only and the AppImage does not reach them either (ADR-0056's glibc 2.35 floor against their 2.34 / 2.28), so both alternatives cost work to serve a population of zero; the CLI is the RHEL 8 / 9 answer and both READMEs now state the per-format split | Accepted (2026-09-05) |
| [0058](0058-the-name-is-protected-separately-from-the-code.md) | The name and logo are held separately from the code licence and governed by `TRADEMARK.md`: nominative use (describing, teaching, repackaging unmodified releases) is free, source-identifying use (product names, official-looking package names, modified builds under the name) needs written permission; the marks are asserted unregistered (™) and registration stays a separate decision | Accepted (2026-09-05) |
| [0059](0059-claude-code-skill-ships-as-a-marketplace-plugin.md) | The Claude Code skill ships as a plugin installed from a `.claude-plugin/marketplace.json` at this repository's root (`packages/claude-code-plugin/skills/chordpro/`), not from `.claude/skills/` and not from a separate repository: co-location keeps the documented commands and the CLI that answers them in one diff, and the plugin version stays in workspace lockstep because Claude Code caches plugins per version and will not re-fetch an unchanged one. The skill is CLI-first; AST JSON is documented against the published `@chordsketch/wasm` package as an optional extra | Accepted (2026-09-05) |
| [0060](0060-wasm-free-chordpro-text-helpers-package.md) | The three ChordPro questions that need no parser — which format a blob is, which of its words are lyrics, what its opening lines look like — ship as a dependency-free `@chordsketch/chordpro-lite`, wasm-free by asserted policy as in ADR-0029; its value-less directive list is generated from a new `BARE_DIRECTIVE_NAMES` in the ADR-0028 catalog and drift-checked on every PR, so a consumer never hand-maintains a fourth copy | Accepted (2026-09-06) |
| [0061](0061-chrome-and-layout-css-ships-from-react-ui.md) | The canonical chrome and layout vocabulary (`.topnav`, `.sidenav`, `.pane` / `.pane-head` / `.pane-body`, `.stack` + modifiers) ships as CSS from the existing `@chordsketch/react-ui/styles.css` rather than a new CSS-only package, with no React binding; the rules are generated from one named canonical reference file per family by `scripts/build-chrome-css.mjs` into a committed `@generated:chrome` region that CI holds at a zero diff, so the shipped stylesheet cannot drift from `design-system/` | Accepted (2026-09-06) |
| [0062](0062-msrv-restatements-are-guarded-by-a-text-check.md) | Every restatement of the workspace `rust-version` outside `Cargo.toml` — the `FROM rust:<tag>@sha256:<digest>` builder pin in every Dockerfile, and README's from-source "Requires Rust N.M or later." sentence — is asserted as text by an unconditional `ci.yml` guard job, because the from-source `Dockerfile` is built by no workflow (`docker.yml` publishes from `Dockerfile.release`) and its pin sat below the floor undetected; a per-PR `docker build .` was rejected for putting an anonymous Docker Hub pull in the path of every pull request | Accepted (2026-09-06) |
| [0063](0063-mcp-server-is-a-cli-subcommand.md) | The Model Context Protocol server ships as a `chordsketch mcp` subcommand of the existing CLI, backed by a new `chordsketch-mcp` library crate speaking the protocol through the official `rmcp` SDK — not as a Node package (ADR-0008 leaves npm publishing manual, so `npx` is unavailable) and not as a second binary (the packaging channels install exactly one, which is why `chordsketch-lsp` reaches no channel today). Six tools cut by what an assistant does with a chart, no PDF and no iReal Pro, and no filesystem or network access: every tool takes ChordPro source as a string | Accepted (2026-09-06) |
| [0064](0064-framework-binding-smoke-tracks-latest.md) | The four framework bindings are listed in README `## Installation` and smoke-tested daily against npm by installing the `latest` dist-tag — the literal documented command — with no version pin, no release-cut probe and no entry in the version-consistency check, because they version off the workspace cadence and a pin on them would have nothing to bump it; each job asserts a server render plus the stylesheet subpath, and `@chordsketch/svelte` renders through a Vite SSR build because it publishes sources | Accepted (2026-09-12) |
| [0065](0065-existence-mode-for-off-cadence-channels.md) | The release-channel manifest gains a third `expected_version`, `"exists"`, asserting a package is still publicly served without asserting which version — for `@chordsketch/chordpro-lite`, which publishes off the workspace cadence and is a helper rather than an install method, so it is absent from README `## Installation` and therefore from `readme-smoke.yml`, leaving `skip` to perform no request at all; the npm check resolves the packument anonymously and fetches the first byte of the tarball `npm install` would download | Accepted (2026-09-13) |
| [0066](0066-preview-handler-shares-tauris-webview2-stack.md) | The Windows preview handler's `wry` / `windows` / `windows-core` requirements stay on the versions Tauri resolves and move together after Tauri, so the installer carries one WebView2 stack; an unconditional `ci.yml` guard compares the two consumers' entries in `Cargo.lock` rather than asserting each crate appears once, because unrelated crates legitimately pull their own copies (`iana-time-zone`'s `windows-core`), and `cargo-deny`'s `bans.multiple-versions` was rejected for needing an allowlist of all 75 duplicated crates — `windows-core` among them | Accepted (2026-09-13) |
| [0067](0067-swift-package-joins-the-release-call-graph.md) | `swift.yml` becomes a `workflow_call` callee of `release.yml`, and the two jobs that consume its XCFramework — `update-cocoapods` and `update-swift-package` — move out of `post-release.yml` into it behind `needs: [publish]`: CocoaPods validation downloaded the zip before it was uploaded (404 on v0.6.0, and the likely cause of the missing 0.4.0) while `Package.swift` polled 30 minutes and timed out behind a full macOS queue; a longer poll, or `post-release` waiting on the whole Swift build, were rejected (amends ADR-0039) | Accepted (2026-09-14) |
| [0068](0068-releases-run-through-one-preflighted-script.md) | A release is `scripts/release.py X.Y.Z`, which pushes no tag until every precondition answerable without publishing holds — release commit at the version, green `ci.yml`, no registry already serving the version, live crates.io token and npm login that own the packages, dry-run packaging of every pending crate and npm package, and every CI credential accepted by its service (`release-credentials.yml`, not a presence check: v0.6.0's Docker Hub and Marketplace tokens were present and expired); local publishing then waits for every CI channel to converge, and the registries rather than a state file decide what a re-run resumes; re-running failed CI channels automatically, presence-only checks, and moving crates.io / npm into CI were rejected | Accepted (2026-09-14; local crates.io / npm publish moved into CI by ADR-0069) |
| [0069](0069-crates-io-and-npm-publish-from-ci-with-trusted-publishing.md) | crates.io and every npm package publish from one `workflow_dispatch`-only workflow, `publish-registries.yml`, through trusted publishing (OIDC) behind `crates-io` / `npm` environments that admit only `main`, instead of from the maintainer's machine: its `check` mode builds and checks every package of the set and proves each registry will mint a token, which `release.py`'s preflight requires before the tag; its `publish` mode replaces the local publish after CI converges and skips what the registries already serve; framework packages dispatch the same workflow; token publishing is closed once it has worked; npm's classic-token revocation and trusted publishing remove both reasons ADR-0008 kept npm local, while new packages still need one manual publish because neither registry attaches a trusted publisher to a name that does not exist; calling it from `release.yml`, granular tokens and separate per-registry workflows were rejected | Accepted (2026-09-15) |
| [0070](0070-publishability-is-checked-on-every-pull-request.md) | A required `Publishable` check runs on every pull request, push to `main` and nightly, with no `paths:` filter: every published package goes through its publish path minus the upload — build, pack, the publish tool's dry run with every warning treated as a failure, artifact inspection (size limits, metadata, entry points, forbidden files, credentials, undeclared files over 1 MiB) and, where it loads without a bundler, an install-and-load smoke — defined once in `scripts/_publish_checks.py`, which `scripts/release.py` imports for its preflight so the release is held to what every pull request was; extending only the release preflight, or requiring the `paths:`-filtered publish workflows, were rejected | Accepted (2026-09-15; pull requests run the checks only when they can reach a package, the merge queue always, by ADR-0079) |
| [0071](0071-aur-ships-a-source-package-and-a-bin-package.md) | The AUR gets two package bases: `chordsketch` builds the tagged source from GitHub's tag archive per the Rust package guidelines, and `chordsketch-bin` repackages the Linux release archive it used to install, because the submission guidelines reserve the plain name for a source build; `update-aur` writes both `.SRCINFO`s with `makepkg --printsrcinfo`, and the `Publishable` check builds the source package in an Arch container and runs `namcap` on the result; renaming the existing package to `-bin`, or keeping one binary package, were rejected | Accepted (2026-09-15) |
| [0072](0072-the-cli-is-not-published-to-flathub.md) | The CLI is not submitted to Flathub, whose requirements do not accept console software, and the never-used `update-flatpak` job, its manifest template and the `flathub` publishability check are removed; a Flathub listing, if any, is the desktop app, which needs its own offline source-build manifest, AppStream metainfo, scoped permissions and first submission; fixing and submitting the CLI manifest, or keeping the dormant job until then, were rejected | Accepted (2026-09-15) |
| [0073](0073-packages-built-on-the-engine-release-with-it.md) | `@chordsketch/react-ui`, `react`, `vue`, `svelte` and `chordpro-lite` carry the workspace version and publish with every release instead of on their own cadence, and the release commit raises the `@chordsketch/wasm` / `wasm-export` pins to the version it releases; the lockfiles of the packages that install `@chordsketch/wasm` link the in-tree `packages/npm`, so that commit installs, tests and packs against the unpublished wasm, which ended the allowlisted pins and follow-up pull request of v0.4.0–v0.6.0; publishing the wasm first and raising the pins in an automatic pull request, one npm workspace, and `file:` or `overrides` in `package.json` were rejected (amends ADR-0064, ADR-0065, ADR-0069) | Accepted (2026-09-15) |
| [0074](0074-the-desktop-app-is-built-for-flathub-from-source.md) | The desktop app is packaged for Flathub as `io.github.koedame.chordsketch`, the GitHub code-hosting ID (Flathub refuses IDs ending in `.desktop`, and `koeda.me` challenges the linter's reachability check; the Tauri identifier stays) on `org.gnome.Platform`, the runtime with WebKitGTK, and built entirely from source offline: Rust with its wasm32 standard library from static.rust-lang.org, the wasm-bindgen CLI from its crate, the tree-sitter grammar with the SDK's clang against wasi-libc headers, npm from the lockfiles, `vite build` and `cargo build --features tauri/custom-protocol`, with source lists generated by `packaging/flatpak/prepare.py`; the Tauri updater is not registered inside a Flatpak; permissions are ipc, wayland, fallback-x11, dri and pulseaudio with files through the portal; a `Publishable` job lints, builds and launches the Flatpak; releases open a pull request on the Flathub repository once it exists, and the first submission is the maintainer's; repackaging the `.deb`, the freedesktop runtime, the rust-stable extension, renaming the Tauri identifier, the npm wasm build, a compile-time updater flag, and the `me.koeda.ChordSketch` domain ID were rejected | Accepted (2026-09-15) |
| [0075](0075-dependabot-groups-patch-and-minor-updates.md) | Dependabot groups each ecosystem's patch and minor version updates into one weekly PR (`cargo-minor-patch`, `actions-minor-patch`) while majors — including every pre-1.0 bump Cargo treats as breaking, which Dependabot classifies as major — and security updates stay one PR per dependency; `/dependabot-review` audits every row of a grouped PR with a per-PR verdict and drops a blocked row with `@dependabot ignore <DEP>`, recording the `unignore` command; ADR-0016's rejection of grouping is reversed for compatible updates because the skip mechanism it lacked now exists, and per-family groups, grouping majors and raising `open-pull-requests-limit` were rejected (amends ADR-0016, ADR-0024) | Accepted (2026-09-15) |
| [0076](0076-linux-jobs-run-on-a-runner-pool-named-by-a-repository-variable.md) | Jobs that need nothing beyond a Linux userland run on `${{ vars.LINUX_RUNNER || 'ubuntu-latest' }}`, a repository variable naming the label of a maintainer-operated pool of ephemeral, unprivileged self-hosted Linux runners (no `sudo`, no container engine, no host mount; fork pull requests from outside collaborators need approval before any workflow runs), because a pull request's 65 Linux jobs waited a median 43 minutes for 20 shared hosted slots while running under a minute each; matrix cells keep `ubuntu-latest` in their name and pick the runner through an `include` key so required checks keep their names; jobs that need `sudo`, docker, `cross`, maturin's manylinux container, snapcraft, flatpak, Homebrew, Swift, a display or `pull_request_target`, and every release / publish / deploy / Claude / `workflow_run` workflow, stay on GitHub-hosted runners; deleting the variable returns every job to GitHub-hosted runners without a commit; a larger plan, writing the label into the workflows, a container engine on the pool, and cutting the job count were rejected or deferred | Accepted (2026-09-16) |
| [0077](0077-dependabot-reads-composite-actions.md) | Dependabot's `github-actions` ecosystem reads `directories: ["/", "/.github/actions/*"]`, because `/` alone skips the composite actions, whose pins had fallen behind the workflows'; measured in a scratch repository, a group still opens one PR across directories but an ungrouped major opens one PR per directory, so an `actions-major` group with `group-by: dependency-name` keeps a major at one PR per action covering every file that pins it; `/dependabot-review` reads the major's versions from its `Updates` line since its title has none; `directories` without the major group, a second `github-actions` entry and inlining the composite actions were rejected (amends ADR-0075) | Accepted (2026-09-16) |
| [0078](0078-the-runner-pool-keeps-a-rust-cache-per-runner.md) | Each runner of the `LINUX_RUNNER` pool keeps a Rust cache directory from its host (cargo registry and one `target/` per cache key and toolchain, one directory per runner slot, emptied weekly or past a size cap) and exports it as `RUST_CACHE_DIR`; Rust jobs cache through `.github/actions/rust-cache`, which links `target/` there when the variable is set and otherwise runs `Swatinem/rust-cache`, because a pool runner spent about 12 of the 17 minutes of `ci.yml`'s four Rust jobs restoring GitHub Actions cache entries at 3.4 MB/s while those entries filled half the 10 GiB budget; a pull request can leave output that a later job reuses, which is accepted because nothing built on the pool is published, and the release VSIX jobs of `vscode-extension.yml` and the bindings jobs of `kotlin.yml` and `ruby.yml` move back to `ubuntu-latest`; turning the cache off, a directory only `main` writes, a GitHub-compatible cache server and sccache were rejected (amends ADR-0076) | Accepted (2026-09-16) |
| [0079](0079-merges-go-through-the-queue-which-runs-the-publish-checks.md) | `main` requires the merge queue again (squash, two groups built at a time, all-green groups, 180-minute check timeout, no up-to-date requirement), and `publishable.yml` runs every job there, on `main`, nightly and on dispatch, while on a pull request a `scope` job runs the publish jobs only when the change touches a path in `PACKAGING_PATHS` or adds a file over 1 MiB, so a pull request that reaches no package waits only for `ci.yml` instead of an extra hour per push and per catch-up; tests keep the path list matching tracked files and covering every workflow and action `publishable.yml` reaches; bot merges enqueue with `gh pr merge` without `--admin` and are done at `MERGED`; keeping the checks on every pull request, running them only in the queue, a `paths:` filter and larger hosted runners were rejected or deferred (supersedes ADR-0015; amends ADR-0013, ADR-0024, ADR-0070) | Accepted (2026-09-17) |
| [0080](0080-swift-package-manifest-at-the-root-pins-the-xcframework-before-the-tag.md) | `Package.swift` moves to the repository root and pins its XCFramework before the tag: `swift.yml` with `pin=X.Y.Z` builds it on the release branch and commits the URL, checksum and generated Swift bindings, the tag-time `publish` uploads that same zip instead of rebuilding, and `Publishable` and `release.py` refuse a tag whose pin is missing, expired or built from other source (amends ADR-0067) | Accepted (2026-09-19) |
| [0081](0081-the-cocoapods-pod-builds-the-tags-swift-sources.md) | The CocoaPods pod takes the release tag as its git source, builds the Swift bindings the tag holds (`source_files`) and fetches the XCFramework in `prepare_command`, refusing a zip that is not the one the tag's `Package.swift` pins, so `pod 'ChordSketch'` provides `import ChordSketch`; the pod used to be the XCFramework zip alone, which holds no Swift API; `swift.yml`'s `lint-podspec` runs `pod lib lint` with the pod's tests for frameworks and static libraries against the XCFramework each pull request builds; dropping CocoaPods, and a second pod for the binary, were rejected (amends ADR-0067, ADR-0080) | Accepted (2026-09-19) |
| [0082](0082-the-desktop-app-has-a-homebrew-formula-that-builds-it-from-source.md) | `koedame/tap` gets a formula `chordsketch-desktop` beside the desktop cask that builds `ChordSketch.app` on the user's machine, so no downloaded app carries the quarantine flag Homebrew puts on every cask download and no Developer ID is needed: Rust from `rustup` (Homebrew's `rust` has no wasm32 standard library), the steps of `desktop-build-steps`, the Tauri CLI through `npx`, an ad-hoc signed bundle, the updater compiled out through `CHORDSKETCH_NO_SELF_UPDATE` (a build-time variable, unlike the Flatpak's runtime check, because a Homebrew build has no runtime signal and a copy in `/Applications` would lose a path test), and `homebrew-desktop-smoke.yml` building it on macOS (6 min 14 s) and launching it through `open`; a Developer ID, a cask that builds, ad-hoc signing the DMG alone, Homebrew's `rust` plus a downloaded wasm32 library and a `Cellar` path test were rejected or left separate; not yet released or documented for users (follows ADR-0074) | Accepted (2026-09-22) |
| [0083](0083-the-macos-quick-look-extension-is-a-data-based-preview-signed-before-the-bundler.md) | The macOS desktop bundle embeds a data-based Quick Look preview extension (`QLPreviewProvider`, macOS 12+) at `Contents/PlugIns/ChordSketchQuickLook.appex`: one Swift file compiled by `swiftc` in a `prebuild` script against `chordsketch-preview-handler` built as a static library through `cargo rustc --crate-type`, so it returns the Windows preview's CSP-carrying document; the app exports `me.koeda.chordsketch.chordpro` (conforming to `public.plain-text`) through a merged `Info.plist` with no document types; the script signs the extension sandboxed with `APPLE_SIGNING_IDENTITY` or ad hoc because the Tauri bundler signs without `--deep` and never signs `bundle.macOS.files`, so a Developer ID must be in a keychain before `npm run build`; PR builds sign ad hoc and the arm64 cell registers and renders it; a view-based `WKWebView` preview, an Xcode project, the UniFFI Swift SDK, a separate crate, `fileAssociations` with `exportedType` and a `--deep` re-sign were rejected (follows ADR-0050) | Accepted (2026-09-24) |
| [0084](0084-the-parser-is-fuzzed-with-cargo-fuzz-from-a-separate-nightly-workflow.md) | The ChordPro parser is fuzzed with cargo-fuzz from `crates/chordpro/fuzz/` (its own workspace, so the zero-dependency crate stays untouched), nightly for 15 minutes and for 30 seconds on a PR that changes the harness | Accepted (2026-09-24) |
