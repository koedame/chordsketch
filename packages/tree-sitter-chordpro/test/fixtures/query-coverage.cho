# Fixture exercising every node the grammar can produce, so that
# `tree-sitter query` in CI resolves each node name referenced by
# queries/*.scm. Keep it in sync with the node-type table in README.md.
{title: Amazing Grace}
{capo 3}
{soc}

A[G]mazing [G/B]grace, how [C]sweet the [G]sound
Lyrics with no chords at all

{start_of_tab}
E|---0---2---3---|
B|---1---3---0---|
{end_of_tab}

{start_of_abc: Refrain}
X:1
K:G
GABc dedB|
{end_of_abc}
